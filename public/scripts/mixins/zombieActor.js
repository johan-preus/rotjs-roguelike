const zombieActor = {
    name: 'ZombieActor',
    groupName: 'Actor',
    act(){
        // find distance to player
        if (distance === 1){
            // attack player
            return
        }
        // move towards player
    }
}